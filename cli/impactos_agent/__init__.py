"""impactOS deterministic CLI package.

A storage-independent, deterministic command-line tool that inventories source
exports, applies confirmed mappings into the impactOS data contract, and produces
the funder-accepted BAI v5 workbook with a provenance manifest.

The tool makes NO language-model calls and holds NO model credentials. All
judgement lives in the harness skills that drive it. See AGENTS.md.
"""

__version__ = "0.1.0"
